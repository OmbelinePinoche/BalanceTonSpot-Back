{% extends 'base.html.twig' %}

{% block main %}
    <h2 class="text-muted fw-bold mb-4">Modifier le spot: {{ spot.name }}</h2>
    {{ form(form) }}
    <a href="{{ path('list') }}">Retour à la liste</a>
    <a href="{{ path('remove', {id:spot.id}) }}">Supprimer ce spot</a>
{% endblock %}