{% extends 'base.html.twig' %}

{% block main %}
	<h2 class="text-muted fw-bold mb-4">{{ spot.name }}</h2>

	<table class="table table-hover mt-4">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ spot.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ spot.name }}</td>
			</tr>
			<tr>
				<th>Ville</th>
				<td>{{ spot.location.name }}</td>
			</tr>
			<tr>
				<th>Adresse</th>
				<td>{{ spot.address }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ spot.description }}</td>
			</tr>
			<tr>
				<th>Image</th>
				<td>{{ spot.picture }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('list_spot') }}">Retour à la liste</a>

	<a href="{{ path('edit_spot', {'id': spot.id}) }}">Modifier</a>

	{# {{ include('back/spot/_delete_form.html.twig') }} #}
{% endblock %}
