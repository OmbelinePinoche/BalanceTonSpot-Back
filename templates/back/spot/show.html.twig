{# {% extends 'base.html.twig' %} #}

{% block title %}Spot{% endblock %}

{% block main %}
    <h1>Spot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spot.id }}</td>
            </tr>
            <tr>
                <th>Nom du spot</th>
                <td>{{ spot.name }}</td>
            </tr>
            <tr>
                <th>Description du spot</th>
                <td>{{ spot.description }}</td>
            </tr>
            <tr>
                <th>Image du spot</th>
                <td>{{ spot.picture }}</td>
            </tr>
            <tr>
                <th>Adresse du spot</th>
                <td>{{ spot.address }}</td>
            </tr>
    
        </tbody>
    </table>

    <a href="{{ path('browse_spot') }}">Retour à la liste</a>

    <a href="">Modifier</a>

    {# {{ include('back/spot/_delete_form.html.twig') }} #}
{% endblock %}