{# {% extends 'base.html.twig' %} #}

{% block title %}utilisateur{% endblock %}

{% block main %}
    <h1>utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>email de l'utilisateur</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>role du l'utilisateur</th>
                <td>{{ user.roles }}</td>
            </tr>
            <tr>
                <th>Mot de passe de l'utilisateur</th>
                <td>{{ user.password }}</td>
            </tr>
            
    
        </tbody>
    </table>

    <a href="{{ path('browse_user') }}">Retour à la liste</a>

    <a href="">Modifier</a>

    {# {{ include('back/user/_delete_form.html.twig') }} #}
{% endblock %}