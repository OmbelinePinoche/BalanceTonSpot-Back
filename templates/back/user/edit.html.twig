{# {% extends 'base.html.twig' %} #}

{% block title %}Modifier un utilisateur{% endblock %}

{% block main %}
    <h1>Modifier un utilisateur</h1>
    {{ form(form) }}
    <a href="{{ path('browse_comment') }}">Retour à la liste</a>
    <a href="{{ path('remove_comment', {id:comment.id}) }}">Supprimer un utilisateur</a>
{% endblock %}