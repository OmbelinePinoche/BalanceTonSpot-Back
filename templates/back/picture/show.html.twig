{# {% extends 'base.html.twig' %} #}

{% block title %}picture{% endblock %}

{% block main %}
    <h1>picture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture.id }}</td>
            </tr>
            <tr>
                <th>Nom du picture</th>
                <td>{{ picture.name }}</td>
            </tr>
            <tr>
                <th>Description du picture</th>
                <td>{{ picture.path }}</td>
            </tr>
            <tr>
                <th>Image du picture</th>
                <td>{{ picture.spot }}</td>
            </tr>
            

        </tbody>
    </table>

    <a href="{{ path('browse_picture') }}">Retour à la liste</a>

    <a href="">Modifier</a>

    {# {{ include('back/picture/_delete_form.html.twig') }} #}
{% endblock %}