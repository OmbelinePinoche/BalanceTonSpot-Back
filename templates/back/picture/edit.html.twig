{% extends 'base.html.twig' %}

{% block title %}Modifier une image{% endblock %}

{% block main %}
    <h1>Modifier une image</h1>
    {{ form(form) }}
    <a href="{{ path('browse_picture') }}">Retour à la liste</a>
    <a href="{{ path('remove_picture', {id:picture.id}) }}">Supprimer une image</a>
{% endblock %}