{% extends 'base.html.twig' %}

{% block main %}
<div class="container-xl bg-light p-5 border-top">

        <div class="row mb-2">

            <h2 class="text-muted fw-bold mb-4">Mes spots favoris</h2>


        {% for spot in favoris %}
            <div class="col-6">

                <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm bg-white position-relative">
        
                </div>
      
            </div>
            {% endfor %}

        </div>

        <div class="col">
            <a href="{{ path('favorites_clear') }}" class="btn btn-danger btn-sm" onclick="return confirm('Vider votre liste de favoris ?');">Vider la liste</a>
        </div>

    </div>
{% endblock %}
